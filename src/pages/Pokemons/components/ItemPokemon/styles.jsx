import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ViewBody: {
        backgroundColor: '#ffffff',
        flexDirection: 'row',
        gap: 5,
        padding: 10,
    }
})